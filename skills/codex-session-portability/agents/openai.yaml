interface:
  display_name: "Codex Session Portability"
  short_description: "Export and import local Codex sessions"
  default_prompt: "Use $codex-session-portability to inspect, export, or import a local Codex session bundle."
