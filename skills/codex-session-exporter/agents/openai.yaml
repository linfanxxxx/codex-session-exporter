interface:
  display_name: "Codex Session Exporter"
  short_description: "Export and import local Codex sessions"
  default_prompt: "Use $codex-session-exporter to inspect, export, or import a local Codex session bundle."
